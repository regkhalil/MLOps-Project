FROM apache/airflow:2.10.5-python3.12

USER root
RUN groupadd -g 1001 dockerhost && usermod -aG dockerhost airflow
USER airflow

# Only install the Docker provider — pipeline code runs in its own container
RUN pip install --no-cache-dir apache-airflow-providers-docker
